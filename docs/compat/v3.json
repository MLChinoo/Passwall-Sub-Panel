{
  "_doc": "PSP × 3X-UI compatibility ranges for the v3.x PSP series. PSP pulls THIS file (docs/compat/v3.json) on demand from any v3.x build because it parses its own version, extracts major=3, and fetches the matching v<MAJOR>.json. Per-major split (v3.6.0-beta.7) keeps each file naturally bounded — when PSP ships v4.0, create v4.json and leave this file frozen. Inside one file, entries is an ordered list and matching is FIRST-MATCH-WINS, so put narrower / newer ranges earlier. Bumping max_tested_xui here ships PSP a wider tested range with zero PSP release. See docs/3xui-compat.md for the human procedure when validating a new 3X-UI release.",
  "schema_version": 2,
  "major": 3,
  "updated_at": "2026-08-10",
  "xui_advisories_doc": "version -> pre-upgrade advisory shown in the panel-upgrade confirm dialog (version.LookupXUIAdvisory). Keyed by the 3X-UI version being upgraded TO; severity is info|warning; affects_xray flags changes that also restart/upgrade the bundled xray-core. Add a new key here to warn admins about a release without shipping a PSP build.",
  "xui_advisories": {
    "3.5.0": {
      "severity": "warning",
      "affects_xray": true,
      "text": "升级 3X-UI 到 3.5.0 会同时把内置 Xray-core 升级到 v26.7.11 并重启 Xray。升级前请确认：① 没有 inbound 同时启用 Final Mask 与 REALITY —— 该组合会让新版 Xray-core 崩溃，3.5.0 已直接拒绝；② MTProto inbound 会自动从单密钥迁移为原生多客户端模型，且 tg:// 链接不再携带 remark 片段；③ 一次数据库迁移会修复溢出的流量计数，并移除 inbound 端口的旧 UNIQUE 约束。Passwall Panel 已对 3.5.0 完成兼容验证（本次已认证），可放心升级。"
    },
    "3.6.0": {
      "severity": "warning",
      "affects_xray": true,
      "text": "升级 3X-UI 到 3.6.0 会同时把内置 Xray-core 升级到 v26.7.28 并重启 Xray。Passwall Panel 已在真实 3.6.0 面板上用自己的适配器完成端到端兼容验证（本次已认证），PSP 侧无需任何改动，可放心升级。升级前请确认以下三点，都只影响「手工配置过 finalmask 的 inbound」，PSP 自己创建的节点不受影响：① **XMC（Minecraft 伪装）掩码格式被上游改破坏性变更** —— Xray-core 26.7.28 把 `finalmask.tcp` 里 xmc 掩码的 `usernames` 换成了 `profiles`（每项需要 username + uuid + texturesValue + texturesSignature），旧格式在 3.6.0 保存时会被直接拒绝。面板不会因此起不来：它会在生成运行配置时自动丢弃这条不完整的掩码并打告警，但该 inbound 会**失去伪装**，需要你手工按新格式重填。② **REALITY inbound 上的 `finalmask.tcp` 会被一次性开机迁移就地删除** —— 这个组合会让 Xray-core 在首个连接时崩溃（XTLS/Xray-core#6453），所以 3.6.0 主动清理它，属于修复。但如果这条 inbound 是被 PSP **导入/接管**的，PSP 存的配置快照会与面板产生漂移，节点的「配置同步」状态会一直停在 `pending` 并反复产生 `inbound_config_push_failed` 审计记录。**订阅内容不受影响**（PSP 只读 `finalmask.udp`），修复办法是在 PSP 的节点编辑器里重新保存一次该节点。③ **REALITY 连接性** —— 本次未发现 26.7.28 有新的 REALITY 回归，但请记住 26.7.11 的教训：若升级后某条 REALITY inbound 的所有客户端突然连不上、且用最新官方 Xray-core 客户端也复现，先检查 `minClientVer` 是不是被新版默认收紧了，显式设成 `\"1.0.0\"` 即可，无需回退版本。"
    }
  },
  "sui_entries_doc": "S-UI counterpart of `entries` (version.CheckSUI). Same first-match-wins matching on [psp_min, psp_max]; fields are min_sui (OPTIONAL) and max_tested_sui (REQUIRED). Unlike min_xui there is no compiled MinSUI backstop to fall back on — see internal/version/compat_sui.go for why that asymmetry is deliberate. While no row matches the running build, ActiveMaxTestedSUI() is \"\", CheckSUI returns unknown, and the Servers page shows NO compat badge for S-UI panels (identical to the behaviour before S-UI gating existed). Adding a verified row turns the gate on for every deployed PSP of that version range with NO PSP release, exactly like bumping max_tested_xui. TO ADD A ROW: validate a real S-UI panel against the endpoints internal/adapters/sui/*.go calls, preferably by pointing TestLive_SUISurface (internal/adapters/sui/client_live_test.go, env-gated on PSP_LIVE_SUI_URL / PSP_LIVE_SUI_TOKEN) at it so the adapter itself is what gets exercised, then append {psp_min, psp_max, max_tested_sui, notes} and state in notes whether the result is LIVE-PANEL VERIFIED or SOURCE-VERIFIED, mirroring the evidence standard the entries below hold themselves to. Do NOT populate this from release notes alone. Mind the pre-release caveat: parseSemver strips -beta.N, so a range cannot separate a fixed beta from a broken one inside the same patch line — when a verification depends on an unreleased adapter fix, start psp_min at the patch release that carries it.",
  "sui_entries": [
    {
      "psp_min": "v3.9.2",
      "psp_max": "v3.99.99",
      "max_tested_sui": "1.5.5",
      "notes": "CEILING 1.5.4 LIVE-PANEL VERIFIED (2026-07-29) against a real S-UI 1.5.4 panel (sing-box v1.13.14, official linux/amd64 release binary) via API-token auth on /apiv2, driven by PSP's own adapter through TestLive_SUISurface in internal/adapters/sui/client_live_test.go — not by a hand-rolled script, so what passed is exactly the code path production uses. Round-tripped: GetServerStatus (sys.appVersion '1.5.4' -> ports.ServerStatus.PanelVersion, the field the compat gate reads; core state running), ListInbounds, ListInboundsSlim, ListClientInbounds, AddInbound -> GetInbound (remark/port verified) -> UpdateInbound (remark+port change reflected) -> list count check -> DelInbound, and the full client lifecycle AddClientToInbounds -> GetClient (InboundIDs contains the inbound) -> UpdateClientWithInbound -> DetachClient -> AttachClient -> DelClientByEmail -> post-delete GetClient returns (nil, nil). psp_min is v3.9.2 DELIBERATELY: this verification also uncovered a hard incompatibility that v3.9.1 and earlier CANNOT create clients through. S-UI unconditionally json.Unmarshal's client.Links on both clients/new and clients/addbulk (service/client.go updateLinksWithFixedInbounds), so PSP omitting the key left it nil and EVERY client create failed with the opaque 'save: unexpected end of JSON input'; modelFromSpec now sends an explicit \"links\":[]. Because parseSemver drops pre-release suffixes, v3.9.1-beta.N (broken) and a fixed v3.9.1-beta.N+1 both canonicalize to 3.9.1 and cannot be told apart by a range — so the floor starts at the patch release carrying the fix, and no already-published build is ever told S-UI 1.5.4 is supported. min_sui is intentionally ABSENT: only 1.5.4 was exercised, nobody has established how far back the /apiv2 contract holds, and CheckSUI reports too_old only against a floor that was actually published. Two S-UI-vs-3X-UI behaviour differences confirmed live and worth remembering: (1) S-UI has no per-inbound enable flag — the adapter rejects Enable=false up front and SetInboundEnable is an unsupported write, so scratch inbounds really do bind; (2) a client miss surfaces as (nil, nil) from GetClient rather than the 'record not found' error the 3X-UI adapter returns. RANGE WIDENED DOWNWARD (2026-07-30): the same TestLive_SUISurface was re-run against SEVEN consecutive S-UI releases, each built from source for darwin/arm64 and each given a FRESH database, panel, admin and seeded token — 1.5.4 (re-confirmed independently of the 2026-07-29 run), 1.5.3, 1.5.2, 1.5.1, 1.5.0, 1.4.2 and 1.4.1. All seven PASS the full surface: GetServerStatus + ListInbounds + ListInboundsSlim + ListClientInbounds, the inbound lifecycle (AddInbound -> GetInbound -> UpdateInbound -> count check -> DelInbound) and the client lifecycle (AddClientToInbounds -> GetClient -> UpdateClientWithInbound -> DetachClient -> AttachClient -> DelClientByEmail -> post-delete GetClient returns (nil,nil)). Panel version was read back from the live panel each time and matched the tag (spot-confirmed on 1.5.4 and 1.4.1). So the /apiv2 contract PSP depends on is now known to be stable across the whole 1.4.1..1.5.4 span, not just the single 1.5.4 point. Two incidental facts worth recording: the token travels in a plain 'Token' HEADER (client.go:88), not Authorization: Bearer, and S-UI stores it in the tokens table as PLAINTEXT with a user_id FK (expiry 0 = never), which is why seeding one directly is a valid bootstrap; also v1.5.2 moved admin passwords to bcrypt, which is irrelevant to token auth. min_sui REMAINS DELIBERATELY ABSENT even though 1.4.1 now passes. Setting min_sui to 1.4.1 would make CheckSUI report too_old for 1.4.0 and below, and that is NOT something this exercise established: 1.4.0 and the 1.3.x line could not be BUILT on this toolchain at all (sing-box's protocol/tailscale endpoint referencing tun.DefaultNIC / tun.NewTCPForwarder / ping.ConnectGVisor, which the sing-tun version Go 1.26.5 resolves no longer exports). That is a local dependency-resolution artifact, NOT a finding about S-UI's API contract — an official 1.4.0 release binary may well work fine. Absent min_sui therefore keeps the honest answer, 'unknown', instead of manufacturing a floor from a compiler error. 1.4.1 is the verified LOWER BOUND OF THE TESTED RANGE, not a published floor. CEILING 1.5.4 -> 1.5.5 (2026-08-10): SOURCE-VERIFIED AND LIVE-PANEL VERIFIED. The v1.5.4..v1.5.5 delta is five commits over seven files, and the decisive fact is that `api/` and `service/` — where the entire /apiv2 surface PSP calls lives — are BYTE-IDENTICAL (git diff over both trees is empty). The only change in a layer PSP touches at all is database/db.go adding `_txlock=immediate` to S-UI's OWN SQLite DSN, which is NET-BENEFICIAL here: it makes read-then-write transactions take the write lock up front instead of failing the lock upgrade with 'database is locked' when the stats job is active (#1209), and PSP's /apiv2 writes were among the callers that could observe those spurious failures. util/genLink.go changed S-UI's naive share-link generation, which is irrelevant because PSP only ever WRITES client.Links (as an explicit `[]`, to satisfy S-UI's unconditional unmarshal on save) and never reads it back — PSP renders its own subscriptions. The rest is the sing-box v1.13.18 / Go 1.26.5 bump, a grpc bump, the release workflow, the frontend submodule pointer and the version file. Live-verified on a real 1.5.5 panel built from source: TestLive_SUISurface (GetServerStatus reporting 1.5.5 with the core running, ListInbounds/Slim/ListClientInbounds, the inbound lifecycle add->get->update->count->del, and the client lifecycle add->get->update->detach->attach->del with the post-delete miss returning (nil,nil)) plus TestLive_SUIBulkSetEnabled (bulk disable and re-enable both read back from the panel, credentials surviving the editbulk full-row write, a second run in the same state writing nothing, and an unknown address reported as skipped rather than counted). min_sui remains absent for the reasons above — 1.5.5 changes nothing about that reasoning."
    }
  ],
  "sui_advisories_doc": "S-UI counterpart of xui_advisories (version.LookupSUIAdvisory), keyed by the S-UI version being upgraded TO. Empty for the same reason as sui_entries.",
  "sui_advisories": {},
  "entries": [
    {
      "psp_min": "v3.9.1",
      "psp_max": "v3.99.99",
      "min_xui": "3.4.2",
      "max_tested_xui": "3.6.0",
      "notes": "v3.9.1 adds node-executed REALITY target discovery by proxying POST /panel/api/server/scanRealityTargets through the selected registered 3X-UI server. That route and its node-side SSRF/concurrency/task bounds first ship in 3X-UI 3.4.2 (absent in 3.4.1), so the operational and compiled floor rise 3.3.0 -> 3.4.2 with no PSP-local fallback. The v3.9.0-only entry immediately below preserves its historical 3.3.0 floor. CEILING 3.5.0 remains LIVE-PANEL VERIFIED; the scanner route is present in both 3.4.2 and 3.5.0 source and its request/response contract is covered by PSP adapter tests. CEILING 3.5.0 -> 3.6.0 (2026-07-30): LIVE-PANEL VERIFIED against a real 3X-UI 3.6.0 panel (xray-core 26.7.28, the exact commit 5ca6f4b pinned by 3.6.0's go.mod) driven through PSP's OWN adapter by the new TestLive_XUISurface + TestLive_XUIRealityScan in internal/adapters/xui/client_live_surface_test.go — not a hand-rolled script, so what passed is the production code path. The panel was built from source for darwin/arm64 and is a FRESH INSTALL; that is the one limitation of this verification and it is why the 3.5.0..3.6.0 delta was ALSO reviewed commit-by-commit for upgrade-only behaviour (see the two watch-items below, both of which a fresh-install smoke structurally cannot observe). Round-tripped: server/status (panelVersion 3.6.0, xray 26.7.28, state running), getPanelUpdateInfo ({channel:stable,currentVersion:3.6.0,latestVersion:v3.6.0,updateAvailable:false} — channel is a new additive key, ignored by Go), getXrayVersion (tag array), getWebCertFiles ({webCertFile,webKeyFile}); inbounds list/list-slim/get/add/update/setEnable/del; clients add (multi-inbound)/get/update-by-email (full-replace: totalGB->2GiB and enable=false both persisted, uuid preserved)/attach/detach/bulkAttach/bulkDetach/bulkCreate ({created:1})/bulkDel ({deleted:1})/del-by-email; the per-node-era single-inbound AddClient/UpdateClient; and scanRealityTargets, which returned a fully-parsed row (Feasible/TLS13/H2/X25519/cert/7 serverNames) — that route defines the 3.4.2 floor, so its health is load-bearing. updatePanel + installXray/:version confirmed route-present (POST, internal/web/controller/server.go:70-71), deliberately not executed. Error wordings PSP string-matches are UNCHANGED and were observed live: post-delete clients/get answers {\"success\":false,\"msg\":\"Obtain (record not found)\"} (isClientNotFoundMsg hits) and a duplicate port answers \"Something went wrong (port 18443 (tcp) already used by inbound '...' (#1) on *\\n)\" (the >=3.4.2 wording PSP already matches at client.go:511 / node.go:947). AUTH: the 3.6.0 release notes' 'openapi.json moved behind session auth' and 'node API tokens write-only' do NOT affect PSP. Bearer-token auth still passes every /panel/api route — proven both by upstream's own new characterization test (internal/web/controller/api_auth_test.go TestCheckAPIAuth_BearerSuccess) and by every live write above; the added mTLS client-cert branch is purely ADDITIVE, api_authed is still set so CSRFMiddleware lets mutations through, and 'node API tokens' refers to 3X-UI's own multi-node credentials, not the panel API token PSP stores. openapi.json did move from the public router to inside the authenticated /panel/api group (api.GET(\"/openapi.json\"), api.Use(checkAPIAuth)) — PSP never fetches it, so zero runtime impact; only the human validation procedure must now present the token. The new ConfigEnvelopeMiddleware on the api group is a pure pass-through unless the caller sends Content-Encoding: zstd or X-Config-Sha256, and PSP sends neither. model.Client.TgID is still int64 (only a gorm index idx_clients_tg_id was added); commit 16b2bcf9 repairs legacy STRING tgId values on upgrade, and PSP has sent tgId as an integer since 3.2.0 (client.go:1227/1235), so PSP-written clients are already on the correct side of that repair. Route table diff across the two tags: exactly ONE new route (GET /clients/get/tgId/:tgId, Telegram lookup, irrelevant to PSP) and ZERO removed. TWO WATCH-ITEMS, both upgrade-only and both confined to inbounds PSP never authors: (1) new boot seeder InboundRealityFinalmaskTcpStrip (internal/database/db.go:1174 gate, :1690 body) rewrites stored stream_settings to delete finalmask.tcp on security==reality inbounds. Fresh installs pre-seed the name at db.go:1081 so it never fires there. On an UPGRADED panel it manufactures drift against a PSP snapshot captured before the upgrade: inboundcfg.InSync compares streamSettings (inboundcfg.go:211), reconcile's axis-A reverse push is on by default (reconcile.go:81), and the push is then rejected forever by validateFinalMaskRealityCombo (inbound.go:1345), leaving config_sync_state=pending plus a repeating inbound_config_push_failed audit row. Subscription OUTPUT is unaffected — PSP reads only finalmask.udp for Hy2 salamander (render/streams.go:32, protocols.go:244-245), never finalmask.tcp — and render simply falls back to a live fetch for that node. Population is near-zero and PSP-created nodes are structurally immune (the SPA emits finalmask only for hysteria2 with tcp: [], which the seeder skips); it needs a hand-made REALITY inbound carrying finalmask.tcp, created while the panel was <=3.4.2, adopted by PSP, then upgraded straight to 3.6.0. Note 3.6.0 is NET-PROTECTIVE here: that combo crashed xray-core on connect (XTLS/Xray-core#6453), so the seeder heals a panel that was already down and the rejected push is what stops PSP restoring the crash config. Cleared by one re-save of the node in PSP's editor. (2) new validateFinalMaskXmcProfiles (inbound.go:773, wired at :921 AddInbound and :1348 UpdateInbound) hard-rejects XMC finalmask masks lacking the new profiles[] shape, because xray-core 26.7.28 replaced XMC.Usernames with XMC.Profiles and dropped the 'Dream' default. No DB migration repairs stored rows; 3X-UI instead strips the mask in-memory at config-generation time (xray.go:319), so the panel still boots and only that inbound loses its obfuscation. PSP has ZERO xmc surface (grep finds no occurrence in any .go/.ts/.tsx), so this can only bite an adopted hand-made inbound. min_xui stays 3.4.2 — 3.6.0 added no capability PSP now requires."
    },
    {
      "psp_min": "v3.9.0",
      "psp_max": "v3.9.0",
      "min_xui": "3.3.0",
      "max_tested_xui": "3.6.0",
      "notes": "v3.9.0 shared-client cutover. FLOOR raised 3.2.0 -> 3.3.0 (MinXUI const bumped in lockstep): the shared-client model attaches ONE client to many inbounds and pushes lifecycle via /clients/update by email, and on 3X-UI 3.2.x that update path fails deterministically with 'UNIQUE constraint failed: client_inbounds.client_id, client_inbounds.inbound_id' (the client_inbounds upsert was fixed in the 3.3 line). 3.2.x panels must upgrade to >= 3.3.0 before migrating to PSP v3.9.0; the v3.6.2-v3.8.99 entry below keeps the old 3.2.0 floor for per-node-era PSP builds that never touch the shared-client path. CEILING 3.4.0 LIVE-PANEL VERIFIED on a real 3.4.0 panel (xray 26.6.22) via API-token auth: the full shared-client surface round-tripped — AddClientToInbounds (multi-inbound) + GetClient ({client:{uuid,...},inboundIds}) + UpdateClient (by email) + AttachClient/DetachClient + DelClientByEmail + BulkDelByEmail, PLUS 6-way same-client and two-*Client-same-backend concurrency with zero client_inbounds corruption (TestLive_* all green); server/status reports panelVersion 3.4.0, getPanelUpdateInfo {current:3.4.0,latest:v3.4.0}, /inbounds/list shapes intact. min_xui stays 3.3.0. CEILING 3.4.0 -> 3.4.1 (2026-06-26): SOURCE/CHANGELOG-VERIFIED against the full v3.4.0...v3.4.1 commit list (NOT yet live-panel — 3.4.0 directly above was live-verified two days prior; 3.4.1 is a patch on it). The delta touches NO endpoint, response shape, data-model struct, DB schema, or auth path PSP uses. Client changes are additive or beneficial: bulk enable/disable + bulk-set XTLS flow (#5524, the commit body states 'no new endpoint, DB column, or migration' — flow is an OPTIONAL bulkAdjust field riding the existing inbound-JSON->SyncInbound path), and #5543 'remove deleted multi-inbound client from runtime regardless of shared email' is a FIX that ALIGNS with PSP's one-shared-client-across-many-inbounds model (improves DelClientByEmail/BulkDelByEmail runtime teardown). All sub-engine / tgbot / native-multi-node / tunnel / outbound / logs-viewer / dev-update-channel changes are in subsystems PSP never calls. model.Client/ClientTraffic/Inbound unchanged; no schema migration on them; no auth/CSRF change. Two NON-BLOCKING watch-items: (1) new VLESS encryption modes (#5517) — if an admin sets one on a VLESS inbound, render may later need to emit the matching client 'encryption' field (future render support, not a break for existing inbounds); (2) #5520 restore-Vision-flow + a one-time idempotent MigrationRestoreVisionFlow boot migration self-heals flow on flow-eligible inbounds (benign for PSP — reconcile re-converges; PSP's own flow derivation already sets Vision where appropriate). min_xui stays 3.3.0. CEILING 3.4.1 -> 3.4.2 (2026-07-01): LIVE-PANEL VERIFIED on a real 3.4.2 panel (xray 26.6.27) via API-token auth. OpenAPI still exposes every route PSP calls: inbounds list/slim/get/add/update/del/setEnable; clients add/get/update/del/attach/detach/bulkCreate/bulkDel/bulkAttach/bulkDetach; server status/getPanelUpdateInfo/updatePanel/installXray/getXrayVersion/getWebCertFiles (updatePanel/installXray route-presence only, not executed). Live smoke round-tripped server/status, updateInfo, xray version list, web cert paths, list/listSlim, two disabled temp inbounds add/setEnable/get/update/del, multi-inbound clients/add, clients/get ({client,externalLinks,inboundIds,usedTraffic}), update-by-email, detach/attach, bulkDetach/bulkAttach, del-by-email, bulkCreate and bulkDel; temp resources cleaned up. Response-shape changes relevant to PSP are additive only (externalLinks sibling on /clients/get, ignored by Go). min_xui stays 3.3.0. CEILING 3.4.2 -> 3.5.0 (2026-07-13): LIVE-PANEL VERIFIED on a real 3.5.0 panel (xray 26.7.11) via API-token auth. All 24 endpoints PSP calls are present in the panel OpenAPI (openapi.json, 166 routes). Read path byte-compatible: server/status (panelVersion 3.5.0, xray.version 26.7.11, state running), getPanelUpdateInfo ({channel(new,ignored),currentVersion:3.5.0,latestVersion:v3.5.0,updateAvailable:false}), getXrayVersion (tag array), getWebCertFiles ({webCertFile,webKeyFile}), /inbounds/list + /inbounds/list/slim (settings/streamSettings/sniffing nested objects, allocate null, clientStats carries id/inboundId/email/up/down/total/enable/expiryTime/reset/lastOnline + additive subId/uuid; slim trims settings.clients to {email,enable}; additive inbound fields originNodeGuid/shareAddr/shareAddrStrategy/subSortIndex/lastTrafficResetTime/trafficReset all ignored), /clients/get ({client:{uuid,email,enable,flow,password,auth,expiryTime,totalGB,...},externalLinks,inboundIds,usedTraffic} — additive siblings ignored, ID still mapped from client.uuid). Write path round-tripped 22/22 on two throwaway DISABLED temp inbounds (existing resources never touched; cleaned up): inbounds add/setEnable/get/update/del; clients add (multi-inbound) / get / update-by-email (full-replace, totalGB->2GiB reflected, enable toggled) / detach / attach / bulkAttach / bulkDetach / bulkDel ({deleted:1}) / bulkCreate ({created:1}) / del-by-email (post-del get answers ' (record not found)', isClientNotFoundMsg hit). updatePanel/installXray route-presence only (not executed). The 3.4.2->3.5.0 delta touches NOTHING in PSP's contract: 'inbounds.port UNIQUE constraint dropped' does NOT change the user-facing AddInbound error — the app-level web/service/port_conflict.go checkPortConflict is BYTE-IDENTICAL across 3.4.2/3.5.0 and AddInbound still returns 'port <p> (<proto>) already used by inbound ...' from it; 'Final Mask + REALITY now rejected' only fires on inbound writes carrying that combo, which PSP never synthesizes (render only READS finalmask for Hy2 obfs); MTProto multi-client, SQLite->Postgres migration, React/uPlot frontend, outbound targetStrategy, and delta-WS stats are all subsystems PSP never calls. model.Client/ClientTraffic/Inbound response shapes changed only additively. min_xui stays 3.3.0. (This bump also fixed a PRE-EXISTING, not-3.5.0-introduced gap: PSP's port-conflict matchers now also recognize the 'already used by inbound' wording that 3X-UI has emitted since >=3.4.2.) CEILING 3.5.0 -> 3.6.0 (2026-07-30): inherits the LIVE-PANEL verification recorded on the v3.9.1+ entry above, performed on a real 3.6.0 panel (xray-core 26.7.28) through PSP's own adapter. v3.9.0 drives the identical shared-client surface — the entry above IS the shared-client path — so nothing is assumed by inheritance here: clients add/get/update-by-email/attach/detach/bulkAttach/bulkDetach/bulkCreate/bulkDel/del-by-email and inbounds list/slim/get/add/update/setEnable/del all round-tripped on 3.6.0, bearer auth is unchanged, and the route diff is +1 (Telegram lookup) / -0. The only difference is that v3.9.0 predates the REALITY scanner, which it never calls. min_xui stays 3.3.0. The two upgrade-only finalmask watch-items described above apply identically and are equally confined to hand-made inbounds PSP never authors."
    },
    {
      "psp_min": "v3.6.2",
      "psp_max": "v3.8.99",
      "min_xui": "3.2.0",
      "max_tested_xui": "3.6.0",
      "notes": "v3.6.2 migrated the 3X-UI client adapter to the first-class /clients/* API. HARD CUT: 3X-UI 3.2.0 removed the inbound-scoped per-client endpoints PSP used through v3.6.1, so v3.6.2+ requires 3X-UI >= 3.2.0 (min_xui floor unchanged). Tested range raised 3.2.0 -> 3.2.7: the whole API surface PSP touches is unchanged across 3.2.0-3.2.7 (every endpoint in adapters/xui/client.go still present), list still returns settings/streamSettings/sniffing as nested objects (flexJSON-compatible) and clientStats still carries email/up/down/total/lastOnline; 3.2.5's unique-subId enforcement is a non-issue because PSP never sets subId (panel auto-generates a unique one). Read path live-verified on a real 3.2.6 panel; /clients/* + inbound write contract is byte-identical to the 3.2.0 live-write baseline. 3.2.7 bump (2026-06-03) is SOURCE-VERIFIED (not yet live-panel): the 44-commit v3.2.6->v3.2.7 delta touches no endpoint PSP calls — web/controller/inbound.go is absent from the changeset (all 7 inbound routes byte-identical), client/server controller changes are purely additive (onlinesByNode, activeInbounds, getWebCertFiles) or doc-only, and the model.Client/ClientTraffic/Inbound structs are unchanged. 3.2.7 hashes API tokens at rest (SHA-256) but Match() hashes the presented bearer token and an ApiTokensHash upgrade migration rewrites existing rows in place, so PSP's stored plaintext bearer token keeps authenticating transparently. 3.2.8 bump (2026-06-05) is LIVE-PANEL VERIFIED on a real 3.2.8 panel (xray 26.6.1): inbound add/get/update/del + client add/get/update/del (all by panel-unique email) + bulkCreate/bulkDel + server/status + getPanelUpdateInfo all round-tripped, and /clients/get returns the unchanged {client:{uuid,email,enable,flow,password,auth,expiryTime,totalGB},inboundIds} shape PSP parses (new additive fields security/group/comment/createdAt/reverse are ignored). The v3.2.7->v3.2.8 delta is multi-node (3X-UI's own master/remote-node feature), client-bulk perf, and subscription-format work; the one client-API-touching change (#4892 'scope remote client update/delete to one inbound') is multi-node-specific and a no-op for standalone PSP, since PSP's per-(panel,inbound)-unique email maps each client to exactly one inbound — confirmed by live update+delete reflecting correctly. Narrower entry first (first-match-wins) so v3.6.2+ overrides the v3.6.0 baseline below. psp_max widened v3.6.99 -> v3.99.99 (2026-06-07): the v3.7.x PSP line (v3.7.0 = account-security + cert multi-account, ZERO 3X-UI adapter changes — adapters/xui/client.go untouched) shares this exact tested range. Without the widen, v3.7.x falls into a 'range gap', RefreshRemoteCompat can't find an entry, and PSP reverts to the compiled 3.2.0 baseline — falsely flagging already-verified 3X-UI versions (3.2.x) as Untested. Keep psp_max at the top of the v3 major; only narrow it by adding a NEWER, more-specific entry ABOVE this one if a future PSP version changes the xui adapter contract. 3.3.0 bump (2026-06-12) is LIVE-PANEL VERIFIED on a real 3.3.0 panel (xray 26.6.1) via API-token auth: every endpoint PSP calls is present and shape-stable. Read path — /inbounds/list + /inbounds/list/slim return settings/streamSettings/sniffing as nested objects (allocate null, flexJSON-tolerated) with clientStats carrying email/up/down/total/enable/expiryTime/reset/lastOnline (additive uuid/subId/inbound-level originNodeGuid/lastTrafficResetTime/trafficReset all ignored by the structs); /clients/get returns {client:{uuid,email,enable,flow,password,auth,expiryTime,totalGB,...},inboundIds} (PSP maps ID from uuid not id). Write path — a full client add->get->update(full-replace, totalGB 1GiB->2GiB reflected)->del round-trip plus bulkCreate ({created:1}) and bulkDel ({deleted:1}) all returned success:true, with post-del /clients/get answering ' (record not found)' (isClientNotFoundMsg hit). server/status (panelVersion 3.3.0), getPanelUpdateInfo ({currentVersion,latestVersion,updateAvailable}), getXrayVersion (array of tag strings), getWebCertFiles ({webCertFile,webKeyFile}) all intact. The 3.2.8->3.3.0 delta is purely ADDITIVE — new endpoints only (clients/groups/*, clients/bulkAdjust|bulkAttach|bulkDetach|delDepleted, custom-geo/*, nodes/*, xray/outbound-subs/*, server/getNew{UUID,VlessEnc,EchCert,mldsa65,mlkem768}) — none remove or change a route PSP touches, so min_xui stays 3.2.0 (MinXUI const/drift-guard untouched). Standalone-PSP caveat unchanged: 3X-UI's native multi-node (inbounds now carry originNodeGuid) stays a no-op because PSP's per-(panel,inbound)-unique email maps each client to exactly one inbound. NOTE: cookie (username/password) login was NOT re-verified on 3.3.0 — token-auth is PSP's verified/recommended path (3.2.x already noted cookie mode needs X-CSRF-Token for unsafe methods; token mode bypasses CSRF). 3.3.1 bump (2026-06-13) is LIVE-PANEL VERIFIED on a real 3.3.1 panel (xray 26.6.1) via API-token auth: all 17 non-destructive endpoints PSP calls round-tripped successfully and the two destructive ones (updatePanel, installXray) are present in the OpenAPI route table. Read path — server/status (panelVersion 3.3.1), getPanelUpdateInfo ({currentVersion:3.3.1,latestVersion:v3.3.1}), getXrayVersion (tag array), getWebCertFiles ({webCertFile,webKeyFile}), /inbounds/list + /inbounds/list/slim (settings/streamSettings/sniffing nested objects, clientStats carries id/inboundId/email/up/down/total/enable/expiryTime/reset/lastOnline; slim trims settings.clients to {email,enable}), /inbounds/get, /clients/get ({client:{uuid,...},inboundIds,usedTraffic} — usedTraffic is a new additive sibling key, dropped by Go; ID still mapped from client.uuid). Write path — inbounds add->get->update(read-modify-write preserving an existing client across a remark change)->setEnable->del all success:true; clients add->get->update(full-replace, totalGB 1GiB->2GiB reflected)->del (post-del get answers ' (record not found)', isClientNotFoundMsg hit), plus bulkCreate ({created:1}) and bulkDel ({deleted:1, new additive skipped[] ignored}); throwaway inbound cleaned up. The 3.3.0->3.3.1 delta (80 commits / 425 files) is purely ADDITIVE from PSP's view, confirmed against source at both tags: the bulk is a source-tree refactor (web/controller/* -> internal/web/controller/*, database/model/* -> internal/database/model/*, git renames 89-96%, invisible over HTTP); model.Client and xray.ClientTraffic are BYTE-IDENTICAL (tgId stays int64/number); model.Inbound only GAINED additive fields (SubSortIndex, ShareAddrStrategy, ShareAddr); the only response-shape change is the additive usedTraffic key on /clients/get; the only new inbound route is POST /inbounds/pushClientTraffics (multi-node, PSP never calls it); the service-layer 'match clients by email / preserve UUID on partial update' fixes align with how PSP already drives update/delete by email. Ships GHSA-jm48 (Xray log-path confinement) — a reason to adopt. No change to adapters/xui/client.go or types.go; min_xui stays 3.2.0 (MinXUI const/drift-guard untouched). 3.4.0 bump (2026-06-24): max_tested 3.3.1 -> 3.4.0. The endpoints the per-node-era PSP calls (/clients/add by inbound, /clients/update + /clients/del by email, /inbounds/list + /inbounds/list/slim, server/status, getPanelUpdateInfo, bulkCreate/bulkDel) are a SUBSET of the shared-client surface live-verified on a real 3.4.0 panel (xray 26.6.22) — full add/get/update-by-email/del + attach/detach + bulkDel all round-tripped, server/status reports panelVersion 3.4.0, /inbounds shapes intact — so the per-node path inherits the verification. psp_max stays v3.8.99 (this entry never drives the shared-client path); v3.9.0+ uses the entry above with min_xui 3.3.0. 3.4.0 -> 3.4.1 bump (2026-06-26): same SOURCE/CHANGELOG analysis as the v3.9.0 entry above — the v3.4.0...v3.4.1 delta touches no endpoint / response shape / model struct / DB schema / auth path the per-node-era PSP calls; the bulk-flow feature is an additive optional bulkAdjust field with no migration, and the multi-inbound-client-delete fix (#5543) is a no-op for the per-node single-inbound path. psp_max + min_xui unchanged. 3.4.1 -> 3.4.2 bump (2026-07-01): same LIVE-PANEL verification on a real 3.4.2 panel via API-token auth; the per-node-era PSP route set is a subset of the smoke above. /clients/add/update/del, bulkCreate/bulkDel, inbounds list/slim/get/add/update/del/setEnable and server read endpoints all round-tripped or stayed route-present with shape-stable additive-only responses. min_xui stays 3.2.0. 3.4.2 -> 3.5.0 bump (2026-07-13): same LIVE-PANEL verification on a real 3.5.0 panel (xray 26.7.11) via API-token auth; the per-node-era PSP route set is a subset of the shared-client smoke above (inbounds list/slim/get/add/update/del/setEnable, clients add/update/del by email, bulkCreate/bulkDel, server read endpoints) — all round-tripped or stayed route-present with additive-only response changes. The 3.4.2->3.5.0 delta touches no endpoint/request/response shape the per-node path uses: web/service/port_conflict.go is byte-identical across 3.4.2/3.5.0, and the Final-Mask+REALITY reject / MTProto multi-client changes are in inbound-write paths PSP never exercises. min_xui stays 3.2.0. 3.5.0 -> 3.6.0 bump (2026-07-30): same LIVE-PANEL verification on a real 3.6.0 panel (xray-core 26.7.28) via API-token auth; the per-node-era route set is a strict subset of the shared-client smoke above and was exercised directly — TestLive_XUISurface drives the legacy single-inbound AddClient/UpdateClient path explicitly, alongside inbounds list/slim/get/add/update/setEnable/del, clients add/update/del by email, and bulkCreate/bulkDel. Bearer-token auth is unchanged (upstream's own TestCheckAPIAuth_BearerSuccess plus every live write); the openapi.json move to the authenticated group and the write-only node API tokens are both irrelevant to PSP, which fetches neither. Route diff across the tags is +1 (GET /clients/get/tgId/:tgId) / -0. min_xui stays 3.2.0. The two upgrade-only finalmask watch-items on the v3.9.1+ entry need no per-node caveat: this era's PSP does not own inbound config snapshots at all, so the reverse-push loop that drives watch-item (1) cannot occur on this line."
    },
    {
      "psp_min": "v3.6.0",
      "psp_max": "v3.6.1",
      "min_xui": "3.1.0",
      "max_tested_xui": "3.1.0",
      "notes": "v3.6.0/v3.6.1 baseline — verified against 3X-UI 3.1.0 (client ops on the inbound-scoped endpoints). Do NOT run these PSP versions against 3X-UI 3.2.0; upgrade PSP to v3.6.2 first."
    }
  ]
}
